<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Book</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #2c3e50, #4ca1af);
            color: #e0e0e0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        .card {
            background: linear-gradient(145deg, #2b2b2b, #1e1e1e);
            color: #ffffff;
            border: 2px solid #444;
            border-radius: 20px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.8);
            animation: fadeInUp 0.7s ease-in-out;
        }

        .card h1 {
            color: #ffcc80; /* Light Orange */
            font-weight: bold;
            text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.5);
        }

        .form-control {
            background-color: #333333;
            color: #ffffff;
            border: 1px solid #555;
            border-radius: 10px;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            box-shadow: 0 0 10px #ffcc80;
            border-color: #ff9800;
        }

        .btn-primary {
            background-color: #ff9800;
            border: none;
            color: #fff;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #e67e22;
            transform: scale(1.05);
        }

        .btn-secondary {
            background-color: #6c757d;
            color: #fff;
            transition: all 0.3s ease;
        }

        .btn-secondary:hover {
            background-color: #5a6268;
            transform: scale(1.05);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="card p-4 shadow-lg mx-auto" style="max-width: 600px;">
            <h1 class="text-center mb-4">Add New Book</h1>
            <form action="/insertrecord" method="post">

                <!-- Book Name -->
                <div class="mb-4">
                    <label for="name" class="form-label">Book Name</label>
                    <input type="text" class="form-control" id="name" name="name" placeholder="E.g., The Great Gatsby" required>
                </div>

                <!-- Book Price -->
                <div class="mb-4">
                    <label for="price" class="form-label">Book Price</label>
                    <input type="number" class="form-control" id="price" name="price" placeholder="E.g., 499" required>
                </div>

                <!-- Book Pages -->
                <div class="mb-4">
                    <label for="pages" class="form-label">Book Pages</label>
                    <input type="number" class="form-control" id="pages" name="pages" placeholder="E.g., 350" min="1" required>
                </div>

                <!-- Book Author -->
                <div class="mb-4">
                    <label for="author" class="form-label">Book Author</label>
                    <input type="text" class="form-control" id="author" name="author" placeholder="E.g., F. Scott Fitzgerald" required>
                </div>

                <!-- Buttons -->
                <div class="d-flex justify-content-between">
                    <button type="submit" class="btn btn-primary px-4">Add Book</button>
                    <a href="/" class="btn btn-secondary px-4">View Books</a>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
