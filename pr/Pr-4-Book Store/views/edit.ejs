<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url('./img/book.jpeg');
            background-size: cover;
            background-color: #121212; /* Dark background fallback */
            color: #e0e0e0;
            font-family: Arial, sans-serif;
            height: 100vh;
            width: 100%;
        }

        .card {
            background-color: #1e1e1e; /* Darker card background */
            color: #e0e0e0; /* Light text */
            border: 1px solid #333;
            border-radius: 15px;
            overflow: hidden;
            transform: scale(0.95);
            opacity: 0;
            animation: fadeInUp 0.6s ease forwards;
            transition: all 0.3s ease;
        }

        .card:nth-child(even) {
            animation-delay: 0.2s;
        }

        .card:nth-child(odd) {
            animation-delay: 0.4s;
        }

        /* Hover Effects for Card */
        .card:hover {
            transform: scale(1.05) translateY(-5px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.8);
            background-color: #2c2c2c; /* Slightly darker on hover */
        }

        .card-title,
        .card-text {
            color: #e0e0e0;
        }

        /* Button Hover Effects */
        .btn {
            transition: all 0.3s ease;
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
        }

        .btn-primary:hover {
            background-color: #0056b3;
            transform: scale(1.1);
        }

        .btn-danger {
            background-color: #dc3545;
            border: none;
        }

        .btn-danger:hover {
            background-color: #b02a37;
            transform: scale(1.1);
        }

        .btn-success {
            background-color: #28a745;
            border: none;
        }

        .btn-success:hover {
            background-color: #218838;
            transform: scale(1.1);
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
        }

        .header h1 {
            font-size: 3rem;
            color: #ffffff;
            animation: fadeInDown 1s ease;
        }

        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-50px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="container w-50 mx-auto mt-5">
        <!-- Header Section -->
        <div class="d-flex justify-content-between align-items-center py-3">
            <h1 class="text-center">Edit Book</h1>
            <a href="/view" class="btn btn-dark" style="width: 80px;">View</a>
        </div>

        <!-- Edit Form -->
        <div class="border rounded shadow-sm p-4">
            <form action="/updateRecord" method="post" >
                <!-- Hidden ID Field -->
                <input type="hidden" name="editid" value="<%= single.id %>">

                <!-- Book Name -->
                <div class="mb-3">
                    <label for="bookName" class="form-label">Book Name:</label>
                    <input type="text" class="form-control" name="name" value="<%= single.name %>" id="bookName" required>
                </div>

                <!-- Book Price -->
                <div class="mb-3">
                    <label for="bookPrice" class="form-label">Book Price:</label>
                    <input type="text" class="form-control" name="price" value="<%= single.price %>" id="bookPrice" required>
                </div>

                <!-- Book Pages -->
                <div class="mb-3">
                    <label for="bookPages" class="form-label">Book Pages:</label>
                    <input type="number" class="form-control" name="pages" value="<%= single.pages %>" id="bookPages" required min="1">
                </div>

                <!-- Book Author -->
                <div class="mb-3">
                    <label for="bookAuthor" class="form-label">Book Author:</label>
                    <input type="text" class="form-control" name="author" value="<%= single.author %>" id="bookAuthor" required>
                </div>

                <!-- Submit Button -->
                <button type="submit" class="btn btn-primary w-100">Update Book</button>
            </form>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
