<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #2c3e50, #4ca1af);
            color: #e0e0e0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .header {
            text-align: center;
            margin-bottom: 1.5rem;
        }

        .header h1 {
            color: #ffcc80;
            font-weight: bold;
            animation: fadeInDown 1s ease;
        }

        .btn-dark {
            transition: transform 0.3s ease, background-color 0.3s ease;
        }

        .btn-dark:hover {
            background-color: #444;
            transform: scale(1.1);
        }

        .form-control:focus {
            border-color: #ffcc80;
            box-shadow: 0 0 5px #ffcc80;
        }

        .btn-primary {
            background-color: #ff9800;
            border: none;
            color: #fff;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #e67e22;
            transform: scale(1.05);
        }

        .card {
            background: #1e1e1e;
            color: #fff;
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
            padding: 2rem;
            width: 100%;
            max-width: 600px;
        }

        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-50px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>
    <div class="card">
        <div class="header">
            <h1> Edit Book</h1>
            <a href="/" class="btn btn-dark">🔙 Back to View</a>
        </div>

        <form action="/updateRecord" method="post">
            <!-- Hidden ID Field -->
            <input type="hidden" name="editid" value="<%= single.id %>">

            <!-- Book Name -->
            <div class="mb-3">
                <label for="bookName" class="form-label"> Book Name:</label>
                <input type="text" class="form-control" name="name" value="<%= single.name %>" id="bookName" placeholder="Enter Book Name" required>
            </div>

            <!-- Book Price -->
            <div class="mb-3">
                <label for="bookPrice" class="form-label">Book Price:</label>
                <input type="text" class="form-control" name="price" value="<%= single.price %>" id="bookPrice" placeholder="Enter Book Price" required>
            </div>

            <!-- Book Pages -->
            <div class="mb-3">
                <label for="bookPages" class="form-label"> Pages:</label>
                <input type="number" class="form-control" name="pages" value="<%= single.pages %>" id="bookPages" placeholder="Enter Number of Pages" required min="1">
            </div>

            <!-- Book Author -->
            <div class="mb-3">
                <label for="bookAuthor" class="form-label"> Author:</label>
                <input type="text" class="form-control" name="author" value="<%= single.author %>" id="bookAuthor" placeholder="Enter Author Name" required>
            </div>

            <!-- Submit Button -->
            <button type="submit" class="btn btn-primary px-4">Update Book</button>
        </form>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
