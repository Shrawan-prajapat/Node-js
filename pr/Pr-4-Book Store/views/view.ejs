<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Store</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #2c3e50, #4ca1af);
            color: #e0e0e0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
        }

        .header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .header h1 {
            color: white;
            font-weight: bold;
            animation: fadeInDown 1s ease;
            margin-bottom: 30px;
        }

        .header .btn-success {
            transition: transform 0.3s ease;
        }

        .header .btn-success:hover {
            transform: scale(1.1);
        }

        .card {
            background: #1e1e1e;
            color: #fff;
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
            transition: all 0.3s ease-in-out;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.8);
        }

        .action-btns .btn {
            transition: all 0.3s ease;
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
        }

        .btn-primary:hover {
            background-color: #0056b3;
            transform: scale(1.1);
        }

        .btn-danger {
            background-color: #dc3545;
            border: none;
        }

        .btn-danger:hover {
            background-color: #b02a37;
            transform: scale(1.1);
        }

        .no-books {
            font-size: 1.5rem;
            color: #ffcc80;
        }

        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-50px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>
    <div class="container my-5">
        
        <!-- Header Section -->
        <div class="header">
            <h1 >View Store</h1>
            <a href="/add" class="btn btn-success btn-lg mt-3"> Add New Book</a>
        </div>

        <!-- Cards Section -->
        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 justify-content-center g-4">
            <% if (alldata && alldata.length > 0) { %>
                <% alldata.forEach((val) => { %>
                    <div class="col">
                        <div class="card h-100 p-3">
                            <div class="card-body">
                                <h5 class="card-title"><strong>📖 :</strong> <%= val.name %></h5>
                                <p class="card-text"><strong>✍️ :</strong> <%= val.author %></p>
                                <p class="card-text"><strong>📄 :</strong> <%= val.pages %></p>
                                <p class="card-text"><strong>💰 :</strong> ₹<%= val.price %></p>
                            </div>
                            <div class="card-footer bg-transparent border-0 d-flex gap-2">
                                <a href="/edituser?id=<%= val.id %>" class="btn btn-primary btn-sm">
                                    ✏️ 
                                </a>
                                <a href="/deleteuser?id=<%= val.id %>" class="btn btn-danger btn-sm">
                                    🗑️
                                </a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <!-- No Data Fallback -->
                <div class="col-12 text-center">
                    <p class="no-books text-black mb-4">No books available.</p>
                    
                </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
